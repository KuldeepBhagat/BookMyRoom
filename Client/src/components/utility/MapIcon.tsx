import { MapIcon} from "../../assets/icons/index"

export const MapLogo = ({className}: {className: string}) => {
    <svg
       className={className}  
    >
        <path d={MapIcon} />
    </svg>
}